<#
.SYNOPSIS
    Require password on resume from sleep (AC power)

.NOTES
    Author          : Salman Nur
    LinkedIn        : linkedin.com/in/salman-nur-112aa225b/
    GitHub          : github.com/salmananur
    Date Created    : 2026-04-01
    Last Modified   : 2026-04-04
    Version         : 1.0
    STIG-ID         : WN11-CC-000150

.TESTED ON
    Date(s) Tested  : 2026-04-01
    Tested By       : Salman Nur
#>

# WN11-CC-000150 - Require password on resume from sleep (AC power)

$regPath = "HKLM:\SOFTWARE\Policies\Microsoft\Power\PowerSettings\0e796bdb-100d-47d6-a2d5-f7d2daa51f51"
$valueName = "ACSettingIndex"
$requiredValue = 1  # 1 = Require password

# Ensure the registry path exists
if (-not (Test-Path $regPath)) {
    New-Item -Path $regPath -Force | Out-Null
}

# Set AC resume password requirement
New-ItemProperty -Path $regPath `
    -Name $valueName `
    -Value $requiredValue `
    -PropertyType DWord `
    -Force | Out-Null

Write-Output "Password on resume from sleep (plugged in) enabled."
